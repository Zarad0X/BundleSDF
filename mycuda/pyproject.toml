[build-system]
requires = ["setuptools>=64", "wheel", "torch>=2.4.0"]
build-backend = "setuptools.build_meta"

[project]
name = "common"
version = "0.0.0"
description = "CUDA extensions for BundleSDF"
requires-python = ">=3.10"
dependencies = ["torch>=2.4.0"] 